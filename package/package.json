{
  "name": "@bindu-js/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Bindu",
    "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.0.0"
  },
  "keywords": ["bindu", "a2a", "agent", "sdk", "typescript"]
}
